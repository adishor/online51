<head>
    <meta charset="UTF-8">
</head>
<body>
    <p>
        {{ 'mail.name'|trans({"%name%" : name}) }}
    </p>
    
    <p>
        {{ 'mail.form-expire-notification.message'|trans
            ({"formName" : formName, 
            "formText" : formText, 
            "expireDate" : expireDate|localizeddate('medium', 'none', locale ) 
        }) }}
    </p>
    <p>
        {{ 'mail.form-expire-notification.message2'|trans }}
    </p>
    <p>
        {{ 'mail.team'|trans }}
    </p>
</body>