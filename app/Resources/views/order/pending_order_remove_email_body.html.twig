<body>
    <p>
        {{ 'mail.pending-order-remove.name'|trans({"%name%" : name}) }}
    </p>
    <p>
        {{ 'mail.pending-order-remove.message'|trans({"%number%" : "%06d"|format(number)}) }}
    </p>
    {% if mentions is defined %}
        <p>
            {{ 'mail.pending-order-remove.other-info'|trans }}</br>
            {{ mentions }}
        </p>
    {% endif %}
</body>